#!/usr/bin/env bash
BUILD_DIR=${1:-}
CACHE_DIR=${2:-}

# inkscape
mkdir -p "$BUILD_DIR/.inkscape/"
cd "$BUILD_DIR/.inkscape/"
wget https://launchpad.net/~inkscape.dev/+archive/ubuntu/stable/+files/inkscape_0.91.0+47~ubuntu14.10.1_amd64.deb
dpkg -i inkscape_0.91.0+47~ubuntu14.10.1_amd64.deb
